<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
  


<button id="rzp-button1">Pay</button>
<input type="hidden" id="dd" value="<%=JSON.stringify(data)%>" />
<script src="https://checkout.razorpay.com/v1/checkout.js"></script>
<script>
    let dd=document.getElementById("dd");
    let data=JSON.parse(dd.value);
    console.log(data,"-==-=-=-=-=-");
var options = {
    "key": "rzp_test_ZSHEm5MP7v8Lqe", // Enter the Key ID generated from the Dashboard
    "amount": data.amount_due, // Amount is in currency subunits. Default currency is INR. Hence, 50000 refers to 50000 paise
    "currency": "INR",
    "name": data.userDetail.name,
    "description": "Test Transaction",
    "image": "https://example.com/your_logo",
    "order_id":data.id, //This is a sample Or //This is a sample Order ID. Pass the `id` obtained in the response of Step 1
    "callback_url": `https://civil-coder-ecom.onrender.com/api/v1/razorpay/payment/res/${data.paymentId}`,
    "prefill": { //We recommend using the prefill parameter to auto-fill customer's contact information especially their phone number
    "name": data.userDetail.name,
        "email": data.userDetail.email,
        "contact": "9000090000" //Provide the customer's phone number for better conversion rates 
    },
    "notes": {
        "address": "Razorpay Corporate Office"
    },
    "theme": {
        "color": "#3399cc"
    }
};
var rzp1 = new Razorpay(options);
document.getElementById('rzp-button1').remove()
// document.getElementById('rzp-button1').onclick = function(e){
    rzp1.open();
    // rzp-button1

    // e.preventDefault();
// }
</script>
</body>
</html>